import React, { ReactElement } from 'react'

interface Props {
    
}

const Console = ({}: Props): ReactElement => {
    return (
        <div>
            Console
        </div>
    )
}
 
export { Console };
export default Console;
